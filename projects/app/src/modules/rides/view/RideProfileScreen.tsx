import { FC } from 'react';
import { ui } from '../../../ui';

export const RideProfileScreen: FC = () => {
    return <RideProfile />;
};

const RideProfile = () => {
    return <ui.Text children='RideProfile' />;
};
